import styled from "@emotion/styled"

export const EnvironmentWrapper = styled.div`
  margin: var(--xxl) auto var(--xl);
  max-width: 468px;
  width: calc(100 % - (var(--md) * 2));
  text-align: center;
`

export const EnvironmentHeading = styled.h4`
  color: var(--primary);
  margin-bottom: var(--xxs);
`

export const EnvironmentList = styled.ul`
  font-size: var(--xs);
  color: var(--mid);
  margin-bottom: 0;
`
